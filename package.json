{
  "name": "karaoke-app",
  "version": "1.0.0",
  "description": "Full-stack karaoke app with YouTube integration and real-time scoring",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"No tests specified\" && exit 0",
    "build": "echo \"No build step required\" && exit 0",
    "lint": "echo \"No linting configured\" && exit 0"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "sqlite3": "^5.1.6",
    "youtube-search": "^1.1.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
